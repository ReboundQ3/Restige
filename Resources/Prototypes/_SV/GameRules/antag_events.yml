- type: entity
  parent: BaseGameRule
  id: SVMalfunctioningAi
  components:
  - type: MalfunctioningAiRule
  - type: StationEvent
    earliestStart: 0
    weight: 8
    minimumPlayers: 15
    maxOccurrences: 1 # can only happen once per round
    duration: null # the rule has to last the whole round not 1 second
  - type: AntagSelection
    agentName: sv-malf-round-end-agent-name
    antags:
    - !type:LinearAntagCount
      proto: SVMalfunctioningAI
      range:
        min: 1
        max: 1
